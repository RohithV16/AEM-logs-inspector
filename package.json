{
  "name": "aem-log-analyzer",
  "version": "1.0.0",
  "description": "AEM Log Analyzer - CLI and Dashboard",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dashboard": "[ -d frontend/dist ] || (npm install --prefix frontend && npm run build --prefix frontend); node src/server.js",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "express": "^4.18.2",
    "jspdf": "^4.2.1",
    "ws": "^8.16.0"
  }
}
